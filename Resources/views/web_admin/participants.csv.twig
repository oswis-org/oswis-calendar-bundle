"ID", "Příjmení", "Křestní jméno", "Celé jméno", "Variabilní symbol", "Akce", "Kategorie", "E-mail", "Telefon", "Velikost trička", "Datum přihlášky", "Aktivováno", "Celková cena", "Zaplaceno [Kč]", "Zbývá [Kč]", "Zaplaceno [%]", "Smazáno"
{% for participant in participants %}
    {{- participant.id|default }}, "{{ participant.contact.familyName }}", "{{ participant.contact.givenName }}", "{{ participant.contact.fullName|default }}", "{{ participant.variableSymbol|default }}", "{{ participant.event.shortName|default(participant.event.name|default) }}", "{{ participant.participantCategory.name|default }}", "{{ participant.contact.email|default }}", "{{ participant.contact.phone|default }}", "{{ participant.tShirt }}", "{{ participant.createdAt ? participant.createdAt|date('Y-m-d H:i') : '' }}", "{{ participant.activated ? participant.activated|date('Y-m-d') : 'Ne' }}", "{{ participant.price|default(0) }}", "{{ participant.paidPrice|default(0) }}", "{{ participant.remainingPrice|default(0) }}", "{{ (participant.paidPricePercentage|default(0) * 100)|replace({'.': ','}) }}", "{{ participant.deletedAt ? participant.deletedAt|date('Y-m-d') : '' }}"
{% endfor %}
