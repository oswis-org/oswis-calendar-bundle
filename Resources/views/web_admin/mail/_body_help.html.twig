{#
    Nápověda pod textem zprávy — společná pro jednotlivou i hromadnou zprávu.
    Parametr: recipientCount (počet přihlášek).
#}
<small class="text-muted">
    {% verbatim %}Proměnné vložíš z nabídky vpravo (např. <code>{{ a }}</code> = koncovka rodu „byl/byla"),
    blok <code>{{ blok('rekapitulace-prihlasky') }}</code>, tlačítko
    <code>&lt;mj-button href="https://…"&gt;Text&lt;/mj-button&gt;</code> (samostatně mezi odstavci).{% endverbatim %}
    Před odesláním se zpráva zkontroluje{{ recipientCount > 1 ? ' pro všech ' ~ recipientCount ~ ' příjemců' : '' }} a vykreslí se pro každého zvlášť.
</small>
