{% extends '@ZakjakubOswisCore/web/page-skeleton-default.html.twig' %}

{% block body_content %}
    <main class="container" id="main-content-container">
        {% if range == 'year' %}
            {# Show calendar for year. #}
        {% elseif range == 'month' %}
            {# Show calendar for month. #}
        {% elseif range == 'week' %}
            {# Show calendar for week. #}
        {% else %}
            {% for event in events %}
                {% include '@ZakjakubOswisCalendar/web/parts/event-preview.html.twig' %}
            {% else %}
                Nebyly nalezeny žádné akce k zobrazení.
            {% endfor %}
        {% endif %}
    </main>
{% endblock %}
